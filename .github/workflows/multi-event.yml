on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main

jobs:
  hello_world:
    runs-on: self-hosted
    steps:
      - name: "Echo Basic Information"
        run: |
          echo "Crazy Fellow"