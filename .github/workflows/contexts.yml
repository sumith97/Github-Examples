name : context-demo
on: [push]

jobs:
  First:
    runs-on: ubuntu-latest
    steps:
      - name: "checking outputs"
        env:
          MY_ACTION: ${{github.action}}
        run: |
          echo "Crazy Fellow"
          echo "$MY_ACTION"